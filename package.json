{
  "name": "paid-content-create",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.3",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "build": "NODE_ENV=production bun scripts/build.ts",
    "start": "bun run build && NODE_ENV=production bun src/server.ts",
    "test": "bun tests/run.ts",
    "check": "bun run test && bun run build"
  },
  "engines": {
    "bun": ">=1.3.3"
  }
}
